# test the code for iteration purposes only.
rev = reversed('spam_points')
iter(rev)
for item in rev:
    print(item)
    print('no value is broken and system is initially at risk!')

# Just a comment, leave a space after this:
class MainObject:
    def __init__(self, item, mark) -> None:
        self.item = item
        self.mark = mark

    def define_point(item):
        if item.lower() == 'gem':
            return 'no image return'
        else:
            return f'{item.define_point} is generated successfully'

    def set_point(set_point):
        return set_point


    def assign_title(title_name, title_address, Is_Broken, Is_Valid):
        while Is_Broken:
            return f'Title address: {title_address}, title_name {title_name}, and '
        else:
            print('no value is defined')
first_instance = MainObject()


# this is a simple python test to verify the validity of the risks involved.
# No result is expected to be returned on the screen.



